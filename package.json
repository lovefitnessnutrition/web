{
  "name": "lfn-web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "pages:dev": "astro build && wrangler pages dev",
    "pages:deploy": "astro build && wrangler pages deploy"
  },
  "dependencies": {
    "astro": "^5.5.0"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
